{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"test": {
			"dependsOn": ["^test"],
			"outputs": []
		},
		"check": {
			"dependsOn": ["^check", "test"],
			"outputs": []
		},
		"check:rust": {
			"inputs": ["crates/**/*.rs", "Cargo.toml", "Cargo.lock"],
			"outputs": []
		}
	}
}
